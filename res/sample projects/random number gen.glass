using System;
using Generic;
using Collections.DataTypes;

int random = Random.range(0,100);
float uniform = Random.Uniform(0, 50);

Shout("Random number: ");
Shoutln(random);

Shout("Uniform number: ");
Shout(uniform);

Get("");
