using System;
using Generic;
using Collections.DataTypes;

int random = Random.rand(0,100);
float uniform = Random.Uniform(1, 200);

Shout("Random number: ");
Shoutln(random);

Shout("Uniform number: ");
Shout(uniform);

Get("");
